/**
 * INTERFACE
 * it helps in grouping variable belonging to one variable, say Object
 * naming is First letter capital letter
 */

interface Point {
  x: number;
  y: number;
}

let drawPoint = (point: Point) => {
  // logic goes here
};

// drawPoint({
//   x: 1,
//   y: 2
// });
